import "./globals.css";

export const metadata = {
  title: "Sunnyside Creative Agency",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`font-barlow antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
